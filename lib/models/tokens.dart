class TokensModel{
  final String? access;

  TokensModel({
    required this.access
  });

  // factory TokensModel.fromJson(Map<String, dynamic> data){
  //   final access = data['accessToken'] as String;

  //   return TokensModel(access: access);
  //}
}